# Distracta
Distracta is a local productivity application that blocks distracting websites during focused work sessions. It programmatically manages the system’s hosts file to restrict configured domains while focus mode is active. Built with Java and Spring Boot, it provides a web-based dashboard for managing domains and sessions locally.
